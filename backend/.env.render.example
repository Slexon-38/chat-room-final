# Backend Environment Variables für Render.com
# Diese Variablen müssen im Render Dashboard unter "Environment" gesetzt werden

# Server Konfiguration
NODE_ENV=production
PORT=10000

# Datenbank (wird von Render PostgreSQL bereitgestellt)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Frontend URL (für CORS)
FRONTEND_URL=https://your-frontend-name.onrender.com

# Optional: Weitere Konfigurationen
# LOG_LEVEL=info
# SESSION_SECRET=your-super-secret-session-key